apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-deployment
  labels:
    app: ci-cd
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ci-cd
  template:
    metadata:
      labels:
        app: ci-cd
    spec:
      nodeSelector:
        node-role.kubernetes.io/worker: "true"
      containers:
      - name: jenkins
        image: jenkins/jenkins:latest
        ports:
        - containerPort: 80
